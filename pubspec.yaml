name: game_counter
description: A basic game counter in Flutter

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  hive: ^2.0.5  
  gap: ^2.0.0
  go_router: ^3.0.1
  intl: ^0.17.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  build_runner: ^2.1.7
  hive_generator: ^1.1.2


flutter:
  uses-material-design: true
