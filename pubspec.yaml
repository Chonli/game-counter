name: game_counter
description: A basic game counter in Flutter

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  hive: ^2.0.5
  freezed_annotation: ^1.1.0
  
  gap: ^2.0.0
  go_router: ^3.0.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  build_runner: ^2.1.7
  freezed: ^1.1.1
  json_serializable: ^6.1.1
  hive_generator: ^1.1.2


flutter:
  uses-material-design: true
